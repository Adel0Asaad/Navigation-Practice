import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    instructionText: {
        flex: 1,
        textAlign: "center",
        color: "white",
        fontSize: 24,
        fontWeight: "bold",
    }
})